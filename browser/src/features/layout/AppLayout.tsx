import Sidebar from "./sidebar/Sidebar";

const AppLayout = () => {
  return (
    <main className="h-screen w-screen p-2 bg-base-300">
      <Sidebar />
    </main>
  );
};

export default AppLayout;
